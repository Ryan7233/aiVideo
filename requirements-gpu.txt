# Optional. Not needed for the clipping pipeline.
#
#   pip install -r requirements-gpu.txt
#
# torch only enables a CUDA fallback check; CTranslate2 already detects a GPU,
# so install this solely if you have a specific reason.
torch>=2.2.0
