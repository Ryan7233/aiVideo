# Ranges rather than exact pins so `pip install -r` works on Python 3.11-3.13;
# requirements.lock.txt records the versions this was verified against.

# --- web ---
fastapi>=0.104.1,<1.0.0
uvicorn[standard]>=0.24.0,<1.0.0
pydantic>=2.5.0,<3.0.0
python-multipart>=0.0.6,<1.0.0
httpx>=0.25.2,<1.0.0
requests>=2.31.0,<3.0.0
aiohttp>=3.9.0,<4.0.0
aiofiles>=23.2.1,<25.0.0

# --- runtime ---
python-dotenv>=1.0.0,<2.0.0
loguru>=0.7.2,<1.0.0
psutil>=5.9.6,<8.0.0

# --- queue (optional: only needed for JOB_BACKEND=celery) ---
celery>=5.3.4,<6.0.0
redis>=5.0.1,<7.0.0
flower>=2.0.1,<3.0.0

# --- media ingest ---
# Platform links (Bilibili, YouTube, ...) are resolved with yt-dlp; a direct
# file URL goes through the size-capped downloader instead.
yt-dlp>=2024.12.6,<2027.0.0

# --- speech & text ---
# faster-whisper runs on CTranslate2, not torch. Word timestamps drive the
# utterance re-segmentation in core/utterances.py.
faster-whisper>=1.0.1,<2.0.0
jieba>=0.42.1,<1.0.0
# Whisper drifts into Traditional part-way through long Mandarin recordings;
# core/chinese.py converts the transcript back. Pure Python, Apache-2.0.
opencc-python-reimplemented>=0.1.7,<0.2.0

# Pillow, numpy, scikit-learn, librosa, soundfile and scipy went with the
# cover, collage and photo-ranking features. Nothing left imports them.
