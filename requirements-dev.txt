# Test and lint dependencies. Not installed into the runtime images.
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest>=7.4.3,<9.0.0
pytest-asyncio>=0.21.1,<2.0.0
pyflakes>=3.0,<4.0
